<UserControl x:Class="NxDataManager.Controls.BackupTaskCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    
    <!-- 任务卡片 -->
    <Border Width="360" Height="Auto" MaxHeight="220" Margin="10"
            Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Cursor="Hand">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect Color="#40000000" BlurRadius="12" 
                                          ShadowDepth="2" Opacity="0.3" Direction="270"/>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect Color="#60000000" BlurRadius="16" 
                                                  ShadowDepth="4" Opacity="0.4" Direction="270"/>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        
        <Border.InputBindings>
            <MouseBinding MouseAction="LeftDoubleClick"
                          Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.EditTaskCommand}"
                          CommandParameter="{Binding}"/>
        </Border.InputBindings>

        <Grid>
            <!-- 顶部彩色条 -->
            <Border Height="6" VerticalAlignment="Top"
                    Background="{DynamicResource PrimaryBrush}"
                    CornerRadius="12,12,0,0"/>

            <StackPanel Margin="16,16,16,12">
                <!-- 标题和状态 -->
                <Grid Margin="0,0,0,10">
                    <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               TextTrimming="CharacterEllipsis"/>
                    
                    <Border Background="{DynamicResource SuccessBrush}" 
                            CornerRadius="4" Padding="8,4"
                            HorizontalAlignment="Right" VerticalAlignment="Top">
                        <TextBlock Text="{Binding Status}" FontSize="11" 
                                   Foreground="White" FontWeight="SemiBold"/>
                    </Border>
                </Grid>

                <!-- 路径信息 -->
                <StackPanel Margin="0,0,0,10">
                    <TextBlock FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}">
                        <Run Text="📁 源:"/>
                        <Run Text="{Binding SourcePath}" FontWeight="SemiBold"/>
                    </TextBlock>
                    <TextBlock FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0">
                        <Run Text="💾 目标:"/>
                        <Run Text="{Binding DestinationPath}" FontWeight="SemiBold"/>
                    </TextBlock>
                </StackPanel>

                <!-- 任务信息标签 -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <Border Background="{DynamicResource AccentBrush}" 
                            CornerRadius="4" Padding="8,4" Margin="0,0,6,0">
                        <TextBlock Text="{Binding BackupType}" FontSize="11" 
                                   Foreground="White" FontWeight="SemiBold"/>
                    </Border>
                    
                    <Border Background="{DynamicResource PrimaryBrush}" 
                            CornerRadius="4" Padding="8,4" Margin="0,0,6,0"
                            Visibility="{Binding EnableCompression, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="🗜️ 压缩" FontSize="11" 
                                   Foreground="White" FontWeight="SemiBold"/>
                    </Border>
                    
                    <Border Background="{DynamicResource WarningBrush}" 
                            CornerRadius="4" Padding="8,4" Margin="0,0,6,0"
                            Visibility="{Binding EnableEncryption, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="🔐 加密" FontSize="11" 
                                   Foreground="White" FontWeight="SemiBold"/>
                    </Border>
                    
                    <Border Background="#9C27B0" 
                            CornerRadius="4" Padding="8,4" Margin="0,0,6,0"
                            Visibility="{Binding EnableVersionControl, Converter={StaticResource BoolToVisibilityConverter}}"
                            ToolTip="{Binding KeepVersionCount, StringFormat='保留 {0} 个版本'}">
                        <TextBlock Text="📂 版本" FontSize="11" 
                                   Foreground="White" FontWeight="SemiBold"/>
                    </Border>
                </StackPanel>

                <!-- 计划任务状态 -->
                <Border Background="#F3F4F6" 
                        BorderBrush="{DynamicResource BorderBrush}" 
                        BorderThickness="1"
                        CornerRadius="6" Padding="8,6" Margin="0,0,0,10"
                        Visibility="{Binding Schedule.Type, Converter={StaticResource NotManualScheduleConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" Text="⏰" FontSize="14" 
                                   VerticalAlignment="Center" Margin="0,0,6,0"/>
                        
                        <StackPanel Grid.Column="1">
                            <TextBlock FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}">
                                <Run Text="计划:"/>
                                <Run Text="{Binding Schedule.Type, Converter={StaticResource ScheduleTypeDisplayConverter}}" 
                                     FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}"/>
                            </TextBlock>
                            <TextBlock FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" 
                                       Margin="0,2,0,0"
                                       Visibility="{Binding NextRunTime, Converter={StaticResource NullToVisibilityConverter}}">
                                <Run Text="下次:"/>
                                <Run Text="{Binding NextRunTime, StringFormat='{}{0:MM-dd HH:mm}'}" 
                                     FontWeight="SemiBold"/>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 底部操作按钮 -->
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="▶️ 运行" 
                                Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.StartBackupCommand}"
                                CommandParameter="{Binding}"
                                Style="{StaticResource IconButtonStyleInCard}"
                                Padding="2,6" FontSize="12" Margin="0,0,6,0"
                                ToolTip="立即运行此备份任务"/>
                        <Button Content="✏️ 编辑"
                                Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.EditTaskCommand}"
                                CommandParameter="{Binding}"
                                Style="{StaticResource IconButtonStyleInCard}"
                                Padding="2,6" FontSize="12"
                                ToolTip="编辑任务配置"/>
                    </StackPanel>

                    <!-- 状态显示（只读） -->
                    <Border HorizontalAlignment="Right" VerticalAlignment="Center"
                            Background="{Binding IsEnabled, Converter={StaticResource BoolToStatusColorConverter}}"
                            CornerRadius="12" Padding="10,4">
                        <TextBlock Text="{Binding IsEnabled, Converter={StaticResource BoolToTextConverter}, ConverterParameter='✓ 启用|✕ 禁用'}" 
                                   FontSize="11" FontWeight="SemiBold"
                                   Foreground="White"/>
                    </Border>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
